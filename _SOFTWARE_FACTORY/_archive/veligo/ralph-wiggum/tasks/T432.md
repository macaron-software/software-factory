# Task T432: Changement theme dynamique

**Priority**: P1
**WSJF Score**: 7.8
**Queue**: TDD
**Category**: WHITE_LABEL
**Original ID**: WL-THEME-001
**Phase**: 🎨 PHASE 3: WHITE-LABEL (Priority P1)
**Section**: 3.1 Thèmes & Personnalisation

## Description
Changement theme dynamique

## Test File
`white-label/theme-dynamic.spec.ts`

## Estimated Files
3 fichier(s) à modifier

## Success Criteria
- [ ] Test E2E créé/modifié: `white-label/theme-dynamic.spec.ts`
- [ ] Test passe en local
- [ ] Pas de test.skip() sans condition
- [ ] Code review adversarial passé
- [ ] Pas de stubs ou TODOs

## Definition of Done (DoD)
- [ ] Feature complètement implémentée
- [ ] Tests E2E verts
- [ ] Conformité AO vérifiée (si applicable)
- [ ] Documentation mise à jour

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: medium
WSJF: 7.8
CATEGORY: WHITE_LABEL
ORIGINAL_ID: WL-THEME-001
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 13:39:56 CET 2026)
```
import type { TenantTheme } from '$lib/theme/types';

const API_BASE = import.meta.env.VITE_API_URL || '/api';

export interface TenantConfigResponse {
  slug: string;
  name: string;
  theme: TenantTheme;
  features: string[];
  modules: string[];
}

export interface TenantListItem {
  slug: string;
  name: string;
  domain: string;
}

/**
 * Fetch 
```

## Last Failure (Wed Jan 14 13:40:33 CET 2026)
```

export interface TenantLegalTexts {
  companyName: string;
  legalNoticeUrl: string;
  privacyPolicyUrl: string;
  termsOfServiceUrl: string;
  cookiePolicyUrl?: string;
  accessibilityStatementUrl?: string;
  copyright?: string;
}

export interface TenantFooterLinks {
  label: string;
  url: string;
  external?: boolean;
}

export interface TenantSocialLinks {
  twitter?: string;
  
```

## Last Failure (Wed Jan 14 13:41:32 CET 2026)
```
bash: OK
      64 veligo-platform/frontend/src/lib/theme/types.ts

---
bash: OK
<script lang="ts">
  import { onMount } from 'svelte';
  import { browser } from '$app/environment';
  import { themes, defaultTheme } from './themes';
  import type { TenantTheme } from './types';
  import { getTenantTheme } from '$lib/api/tenants';
  import { loadTenantCSS, type TenantSlug, isValidTenant } from '$lib/themes';

  /**
   * ThemeProvider Component
   *
   * Loads and applies tenant-specific theming.
   *
   * Note: Theme fetching currently uses REST API via getTenantTheme().
   *
```
