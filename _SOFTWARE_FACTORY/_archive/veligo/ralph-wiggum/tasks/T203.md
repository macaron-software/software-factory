# Task T203: Implement UpdateBikeStatus gRPC method

**Priority**: P0
**Queue**: TDD
**Type**: implementation

## File
veligo-platform/backend/src/grpc/services/bike.rs

## Description
Implement UpdateBikeStatus to change status (available, reserved, in_use, maintenance, out_of_service, stolen)

## Success Criteria
- [ ] Implementation complete
- [ ] cargo check passes
- [ ] Unit tests added
- [ ] No new warnings

## Ralph Status Block
---RALPH_STATUS---
STATUS: COMPLETED
COMPLEXITY: medium
WSJF: 12
---END_RALPH_STATUS---

## Last Failure (Tue Jan 13 23:50:48 CET 2026)
```
```bash
sed -n '1,50p' veligo-platform/backend/src/grpc/services/bike.rs
```

=== Action Results ===
bash: OK
// Bike gRPC Service Implementation
// PR-003: Bike gRPC Service
// Date: 2026-01-03

use sqlx::{PgPool, Row};
use std::sync::Arc;
use tonic::{Request, Response, Status};
use uuid::Uuid;

// Import generated proto types from parent module (tonic-server.rs)
// Since this file is included via #[path], we use the parent's proto module
use crate::proto::bike::v1::bike_service_server::BikeService;
use crate::proto::bike::v1::{
    Bike as ProtoBike, BikeStatus as ProtoBikeStatus, BikeType as ProtoBik
```

## Last Failure (Tue Jan 13 23:51:53 CET 2026)
```
  string station_id = 1;
  string tenant_id = 2;
  optional BikeStatus status = 3;
}

message ListByStationRequest {
  string station_id = 1;
  optional BikeStatus status = 2;
  optional BikeType bike_type = 3;
}

message GetRequest {
  string bike_id = 1;
}

message ReserveBikeRequest {
  string bike_id = 1;
  string user_id = 2;
  string tenant_id = 3;
  in
```

## Last Failure (Tue Jan 13 23:52:51 CET 2026)
```
```bash
sed -n '1,100p' veligo-platform/backend/src/grpc/services/bike.rs > /tmp/bike_part1.txt && cat /tmp/bike_part1.txt
```

=== Action Results ===
bash: OK
// Bike gRPC Service Implementation
// PR-003: Bike gRPC Service
// Date: 2026-01-03

use sqlx::{PgPool, Row};
use std::sync::Arc;
use tonic::{Request, Response, Status};
use uuid::Uuid;

// Import generated proto types from parent module (tonic-server.rs)
// Since this file is included via #[path], we use the parent's proto module
use crate::proto::bike::v1::bike_service_server::BikeService;
use crate::proto::bike::v1::{
    Bike as ProtoBike, BikeStatus as ProtoBikeStatus, BikeType as ProtoBik
```
