# Task T1280: Ensure all tests pass with injected dependencies

**Priority**: P1
**WSJF Score**: 8.0
**Queue**: TDD
**Parent**: T1154
**Generated by**: Fractal LLM Delegation

## Description
Ensure all tests pass with injected dependencies

## Reason for Delegation
Task requires refactoring PaymentAdapterFactory and related components across 4+ files (PaymentAdapterFactory, StripeProcessor, and test files), involving dependency injection, factory pattern, and test isolation. Touches multiple domains and exceeds 400 LOC.

## Success Criteria
- [ ] Task completed
- [ ] Tests pass
- [ ] No stubs or TODOs

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: medium
PARENT_TASK: T1154
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 21:00:35 CET 2026)
```
use async_trait::async_trait;
use thiserror::Error;

use super::stripe_processor::{StripeProcessorTrait, StripeResult, Charge};

/// Payment adapter errors
#[derive(Debug, Error)]
pub enum PaymentAdapterError {
    #[error("Stripe error: {0}")]
    StripeError(String),
    
    #[error("Invalid request: {0}")]
    InvalidRequest(String),
    
    #[error("Payment not found: {0}")]
    NotFound(String),
}

/// Result type for payment adapter operations
pub type PaymentResult<T
```

## Last Failure (Wed Jan 14 21:01:35 CET 2026)
```
pub use paypal::{PayPalAdapter, PayPalProcessor};
pub use stripe::{StripeAdapter, StripeProcessor};
---
bash: OK
use thiserror::Error;
use crate::payment::PaymentProvider;

/// Errors that can occur during payment operations
#[derive(Debug, Error, Serialize, Deserialize)]
pub enum PaymentError {
    #[error("Provider {provider} not supported")]
    ProviderNotSupported { provider: PaymentProvider },
    
    #[error("Payment failed: {message}")]
    PaymentFailed { message: String },
    
    #[error("Refund failed: {message}")]
    RefundFailed { message: String },
    
    #[error("Invalid configuration:
```

## Last Failure (Wed Jan 14 21:07:20 CET 2026)
```
    ... which satisfies dependency `sqlx-sqlite = "=0.8.0"` of package `sqlx v0.8.0`
    ... which satisfies dependency `sqlx = "^0.8"` of package `veligo-auth-service v0.1.0 (/Users/sylvain/_LAPOSTE/_VELIGO2/veligo-platform/services/veligo-auth-service)`
versions that meet the requirements `^0.28.0` are: 0.28.0

package `libsqlite3-sys` links to the native librar
---
bash: OK
    Updating crates.io index
error: failed to select a version for `libsqlite3-sys`.
    ... required by package `sqlx-sqlite v0.8.0`
    ... which satisfies dependency `sqlx-sqlite = "=0.8.0"` of package `sqlx v0.8.0`
    ... which satisfies dependency `sqlx = "^0.8"` of package `veligo-auth-service v0.1.0 (/Users/sylvain/_LAPOSTE/_VELIGO2/veligo-platform/services/veligo-auth-service)`
versions that meet the requirements `^0.28.0` are: 0.28.0

package `libsqlite3-sys` links to the native librar
---
bash: FAIL

---
bash: OK
```
