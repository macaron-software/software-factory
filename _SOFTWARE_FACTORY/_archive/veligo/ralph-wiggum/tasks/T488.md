# Task T488: Couleurs personnalisables

**Priority**: P2
**WSJF Score**: 6.5
**Queue**: TDD
**Category**: WHITE_LABEL
**Original ID**: WL-THEME-003
**Phase**: 🎨 PHASE 3: WHITE-LABEL (Priority P1)
**Section**: 3.1 Thèmes & Personnalisation

## Description
Couleurs personnalisables

## Test File
`white-label/custom-colors.spec.ts`

## Estimated Files
2 fichier(s) à modifier

## Success Criteria
- [ ] Test E2E créé/modifié: `white-label/custom-colors.spec.ts`
- [ ] Test passe en local
- [ ] Pas de test.skip() sans condition
- [ ] Code review adversarial passé
- [ ] Pas de stubs ou TODOs

## Definition of Done (DoD)
- [ ] Feature complètement implémentée
- [ ] Tests E2E verts
- [ ] Conformité AO vérifiée (si applicable)
- [ ] Documentation mise à jour

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: low
WSJF: 6.5
CATEGORY: WHITE_LABEL
ORIGINAL_ID: WL-THEME-003
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 14:19:04 CET 2026)
```
  bgAccentSubtle: 'var(--ds-primary-50)',
  bgSuccess: '#dcfce7',          
---
bash: OK
/**
 * Dark Theme Semantic Colors
 * All colors meet WCAG AA 4.5:1 contrast ratio
 */

import type { SemanticColors } from '../tokens/types';

export const darkTheme: SemanticColors = {
  // Backgrounds
  bgPrimary: '#0f172a',           // Slate-900
  bgSecondary: '#1e293b',         // Slate-800
  bgTertiary: '#334155',          // Slate-700
  bgInverse: '#f8fafc',           // Slate-50
  bgAccent: 'var(--ds-primary-500)',
  bgAccentSubtle: 'var(--ds-primary-950)',
  bgSuccess: '#14532d',       
```

## Last Failure (Wed Jan 14 14:21:27 CET 2026)
```

---
bash: OK
/**
 * E2E Test: Theme Marketplace (Owner Dashboard)
 * 
 * User: Owner (La Poste SuperAdmin)
 * URL: https://admin.veligo.app/marketplace/themes
 * 
 * Status: 🔴 RED (Not implemented)
 */

import { test, expect } from '@playwright/test';

test.describe('[RED] Theme Marketplace - Owner Dashboard', () => {
  
  test.beforeEach(async ({ page }) => {
    await page.goto('https://admin.veligo.app/login');
    await page.fill('[data-testid="email"]', 'owner@laposte.fr');
    await page.fill('[data-te
```

## Last Failure (Wed Jan 14 14:23:34 CET 2026)
```
</think>

<bash>
cat veligo-platform/frontend/src/lib/stores/tenant.ts</bash>
<bash>
find . -path "*/design-system*" -name "*.ts" | xargs grep -l "createTheme" 2>/dev/null | head -5</bash>

=== Action Results ===
bash: OK
import { writable, derived, get } from 'svelte/store';
import { browser } from '$app/environment';
import { getTenantConfig, getTenantTheme } from '$lib/api/tenants';
import { themes, defaultTheme } from '$lib/theme/themes';
import type { TenantTheme } from '$lib/theme/types';
import { moduleClient, grpcCall } from '$lib/grpc-client';
import type { TenantModule as GrpcTenantModule, Module as GrpcModule } from '$lib/grpc/module/v1/module';

// =====================================================
---
bash: OK
```

## Last Failure (Wed Jan 14 14:24:26 CET 2026)
```
  url: string;
  external?: boolean;
}

export interface TenantSocialLinks {
  twitter?: string;
  

=== LLM Response ===
<think>
The types.ts file only shows 64 lines but I can only see part of it. Let me try to get the full file content. It seems like the output is being truncated. Let me try a different approach.
</think>

<bash>
cat -A veligo-platform/frontend/src/lib/theme/types.ts | tail -30</bash>

=== Action Results ===
bash: OK
cat: illegal option -- A
usage: cat [-belnstuv] [file ...]
```

## Last Failure (Wed Jan 14 14:25:48 CET 2026)
```
 * Run with: npx playwright test tests/e2e/admin.spec.ts
 */

import { test, expect, type Page } from '@playwright/test';

const BASE_URL = process.env.BASE_URL || 'http://localhost:5173';

// Mock admin user
const mockAdmin = {
  id: 'admin-001',
  email: 'admin@veligo.fr',
  firstname: 'Marie',
  lastname: 'Administrateur',
  role: 'admin',
  tenant_id: 'tenant-idfm',
  permissions: [
    'users:read',
    'users:write',
    'users:delete',
   
```

## Last Failure (Wed Jan 14 14:27:18 CET 2026)
```
bash: OK
import type { TenantTheme } from './types';

// Default theme defined first for SSR safety
export const defaultTheme: TenantTheme = {
  slug: 'default',
  name: 'Veligo Platform',
  tagline: 'Location velo longue duree',
  colors: {
    primary: '#0066CC',
    primaryHover: '#0052A3',
    secondary: '#00A651',
    accent: '#FFD700',
    background: '#FFFFFF',
    surface: '#F5F5F5',
    text: '#1A1A1A',
    textMuted: '#666666',
    error: '#DC3545',
    success: '#28A745',
    warning: '#FFC107
```
