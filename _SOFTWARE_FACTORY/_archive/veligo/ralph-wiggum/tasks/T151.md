# TDD Task - TAN Card Integration Component

## Anomaly
Tests expect TAN (Nantes) card integration selectors (`tan-card-number`, `tan-connection`) that don't exist in frontend components.

## Location
- Tests: `tests/journeys/17-multimodal-integration.spec.ts`, `tests/journeys/booking.journey.spec.ts`
- Missing component: Nantes TAN card integration UI

## Steps

### Phase 1: Create TAN Card Integration Component (RED)
```bash
# Create component file
touch src/lib/components/tan/TanCardIntegration.svelte
```

### Phase 2: Implement TAN Card Selectors
```svelte
<!-- src/lib/components/tan/TanCardIntegration.svelte -->
<script lang="ts">
  let { cardNumber = '', linked = false, connectionStatus = 'disconnected' }: Props = $props();
</script>

<div data-testid="tan-connection" class="tan-connection-section">
  <span data-testid="tan-card-number">{cardNumber}</span>
</div>
```

### Phase 3: Add Component to Nantes Dashboard
```bash
# Find Nantes dashboard route
ls src/routes/\(tenant\)/nantes/
```

### Phase 4: Verify Tests Pass
```bash
cd veligo-platform/frontend && npm run test:journey -- --grep "Nantes"
```

## Acceptance Criteria
- [ ] `data-testid="tan-connection"` renders on Nantes tenant
- [ ] `data-testid="tan-card-number"` displays card info
- [ ] All TAN tests pass without `.skip()`
