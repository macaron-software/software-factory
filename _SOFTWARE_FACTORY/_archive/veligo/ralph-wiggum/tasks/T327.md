# Task T327: Implement /themes admin page

**Priority**: P2
**Queue**: TDD
**Type**: implementation

## File
veligo-platform/frontend/src/routes/(admin)/themes/+page.svelte

## Description
Implement theme customization page for tenant branding: colors, logos, custom CSS

## Success Criteria
- [ ] Implementation complete
- [ ] cargo check passes (or npm run build for frontend)
- [ ] Unit/integration tests added
- [ ] No new warnings
- [ ] Works with gRPC calls (frontend) or database queries (backend)

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: low
WSJF: 6
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 10:09:02 CET 2026)
```
      accent: string;
      [key: string]: string;
    };
    typography: {
      font_family_primary: string;
      base_size_px: number;
    };
  }
  
  let desig
---
bash: OK
import { GrpcWebFetchTransport } from '@protobuf-ts/grpcweb-transport';
import type { UnaryCall } from '@protobuf-ts/runtime-rpc';
import { AuthServiceClient } from './grpc/auth/v1/auth.client';
import { ModuleServiceClient } from './grpc/module/v1/module.client';
import { SubscriptionServiceClient } from './grpc/subscription/v1/subscription.client';
import { StationServiceClient } from './grpc/station/v1/station.client';
import { BookingServiceClient } from './grpc/booking/v1/booking.client';
i
```

## Last Failure (Wed Jan 14 10:09:41 CET 2026)
```
}

#[async_trait]
impl ThemeRepository for PostgresThemeRepository {
    async fn find_by_tenant(&self, tenant_id: Uuid) -> Result<DesignSystem, ThemeError> {
    
---
bash: OK
// Veligo Tenant API - Multi-tenant management endpoints
// Date: 2025-12-26
//
// Provides REST API for tenant and module management:
// - GET /api/tenants/:id - Get tenant info
// - GET /api/tenants/:id/modules - List enabled modules for tenant
// - PUT /api/tenants/:id/modules/:module_id - Enable/disable module
// - GET /api/tenants/resolve - Resolve domain to tenant

use crate::infrastructure::http::middleware::{require_admin, require_auth};
use axum::{
    extract::{Extension, Path, Query},
```

## Last Failure (Wed Jan 14 10:10:11 CET 2026)
```

=== LLM Response ===
<think>
The output seems truncated. Let me try to see the full file in parts.
</think>

```bash
wc -l veligo-platform/frontend/src/lib/grpc-client.ts && cat veligo-platform/frontend/src/lib/grpc-client.ts | tail -100
```

=== Action Results ===
bash: OK
      90 veligo-platform/frontend/src/lib/grpc-client.ts
import { GrpcWebFetchTransport } from '@protobuf-ts/grpcweb-transport';
import type { UnaryCall } from '@protobuf-ts/runtime-rpc';
import { AuthServiceClient } from './grpc/auth/v1/auth.client';
import { ModuleServiceClient } from './grpc/module/v1/module.client';
import { SubscriptionServiceClient } from './grpc/subscription/v1/subscription.client';
import { StationServiceClient } from './grpc/station/v1/station.client';
import { BookingS
```

## Last Failure (Wed Jan 14 10:10:12 CET 2026)
```

=== Action Results ===
bash: OK
total 24
drwx------   5 sylvain  staff   160 Dec 28 01:43 .
drwxr-xr-x  19 sylvain  staff   608 Jan  9 08:09 ..
-rw-------   1 sylvain  staff   279 Dec 28 01:43 mod.rs
-rw-------   1 sylvain  staff  2688 Dec 28 01:41 repository.rs
-rw-------   1 sylvain  staff  1017 Dec 28 01:40 types.rs

---
bash: OK
// Trace: 2025-12-28 - Admin Domain Module (Hexagonal Architecture)
// Reference: Refactoring API to use Repositories

mod repository;
mod types;

pub use repository::{AdminBike, AdminRepository, AdminRepositoryError, AdminUser};
pub use types::{DashboardMetrics, TenantModule};
```

## Last Failure (Wed Jan 14 10:10:52 CET 2026)
```
    TenantConfigRepository,
};


---
bash: OK
/**
 * Base gRPC-Web client setup using @connectrpc/connect-web
 * 
 * PR-006: Frontend gRPC-Web Client Setup
 * This module provides the base transport and client configuration for gRPC-Web calls.
 */

import { createConnectTransport } from '@connectrpc/connect-web';
import type { Transport } from '@connectrpc/connect';

// gRPC-Web endpoint - routes to tonic-server via nginx proxy
const GRPC_BASE_URL = import.meta.env.VITE_GRPC_URL || 
  (typeof window !== 'undefined' 
    ? `${window.location
```

## Last Failure (Wed Jan 14 10:11:35 CET 2026)
```
use serde::{Deserialize, Serialize};
use uuid::Uuid;

/// Design System aggregate root
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DesignSystem {
    pub id: Uuid,
    pub tenant_id: Uuid,
    pub version: String,
    pub brand: BrandConfig,
    pub colors: ColorPalette,
    pub typography: Typography,
    pub spacing: Spa
---
bash: OK
     330 veligo-platform/backend/src/domain/theme/design_system.rs

---
bash: OK
     765
```
