# TDD Task - Stations Route Implementation

## Anomaly
Tests navigate to `/stations` route which doesn't exist in the frontend routes structure.

## Location
- Tests: `tests/journeys/booking.journey.spec.ts`, `tests/journeys/14-station-search.spec.ts`
- Route: `src/routes/(app)/stations/+page.svelte` (missing)

## Steps

### Phase 1: Create Stations Route (RED)
```bash
# Create stations route directory
mkdir -p src/routes/\(app\)/stations
touch src/routes/\(app\)/stations/+page.svelte
touch src/routes/\(app\)/stations/+page.ts
```

### Phase 2: Implement Stations Page with Required Selectors
```svelte
<!-- src/routes/(app)/stations/+page.svelte -->
<script lang="ts">
  import StationMap from '$lib/components/stations/StationMap.svelte';
  import StationCard from '$lib/components/StationCard.svelte';
</script>

<div data-testid="stations-page" class="stations-page">
  <div data-testid="stations-map" class="stations-map">
    <StationMap />
  </div>
  <div data-testid="station-list" class="station-list">
    <!-- station cards -->
  </div>
  <input data-testid="station-search" placeholder="Rechercher une station" />
  <button data-testid="station-filter">Filtrer</button>
</div>
```

### Phase 3: Verify Routes Work
```bash
cd veligo-platform/frontend && npm run dev &
# Navigate to http://localhost:5173/stations
```

### Phase 4: Verify Tests Pass
```bash
cd veligo-platform/frontend && npm run test:journey -- --grep "stations"
```

## Acceptance Criteria
- [ ] `/stations` route renders without 404
- [ ] `data-testid="stations-page"` exists
- [ ] `data-testid="stations-map"` contains map
- [ ] `data-testid="station-list"` contains stations
- [ ] `data-testid="station-search"` input exists
- [ ] `data-testid="station-filter"` button exists
- [ ] All station tests pass
