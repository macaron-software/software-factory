# Task T486: Chat support en temps réel

**Priority**: P2
**WSJF Score**: 6.5
**Queue**: TDD
**Category**: JOURNEY
**Original ID**: J-E2E-017
**Phase**: 🚴 PHASE 1: JOURNEYS E2E ESSENTIELS (Priority P0)
**Section**: 1.4 Incidents & Support

## Description
Chat support en temps réel

## Test File
`journeys/117-live-support.spec.ts`

## Estimated Files
3 fichier(s) à modifier

## Success Criteria
- [ ] Test E2E créé/modifié: `journeys/117-live-support.spec.ts`
- [ ] Test passe en local
- [ ] Pas de test.skip() sans condition
- [ ] Code review adversarial passé
- [ ] Pas de stubs ou TODOs

## Definition of Done (DoD)
- [ ] Feature complètement implémentée
- [ ] Tests E2E verts
- [ ] Conformité AO vérifiée (si applicable)
- [ ] Documentation mise à jour

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: medium
WSJF: 6.5
CATEGORY: JOURNEY
ORIGINAL_ID: J-E2E-017
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 14:17:32 CET 2026)
```
/**
 * E2E Journey Tests - Support Tickets
 * Complete support ticket workflow
 */

import { test, expect, type Page } from '@playwright/test';

const BASE_URL = process.env.BASE_URL || 'http://localhost:5173';
const API_URL = process.env.API_URL || 'http://localhost:8888';

const TEST_USER = {
  email: 'support-test@example.com',
  password: 'SupportTest123!'
};

const SEL = {
  // Login
  loginEmail: '[data-testid="login-email"]',
  loginPassword: '[data-testid="login-password"]',
  loginSubmi
```

## Last Failure (Wed Jan 14 14:18:38 CET 2026)
```
  loginSubmi
---
bash: OK
/**
 * E2E Journey Tests - Registration Complete (36 tests)
 * TDD Red Phase - Full registration flow with RGPD consent
 *
 * Personas tested:
 * - New IDFM user (Paris/Île-de-France)
 * - New Nantes user (Naolib white-label)
 * - New Lyon user (TCL integration)
 *
 * Journeys covered:
 * 1. Form display and validation
 * 2. RGPD consent requirements
 * 3. Password strength validation
 * 4. Email verification flow
 * 5. Profile completion
 * 6. First subscription selection
 * 7. Tenant-specific 
```

## Last Failure (Wed Jan 14 14:19:32 CET 2026)
```
The output is being truncated. Let me try to get the full file content using a different approach.
</think>

```bash
tail -200 ./veligo-platform/frontend/tests/journeys/10-support-tickets.spec.ts
```

=== Action Results ===
bash: OK
      await page.fill(SEL.ticketDescription, 'Je constate une erreur sur ma facture du mois de décembre. Le montant prélevé ne correspond pas à mon abonnement.');
      await page.selectOption(SEL.ticketPrioritySelect, 'high');

      await page.click(SEL.ticketSubmit);

      await expect(page.locator(SEL.toastSuccess)).toBeVisible();
      await expect(page.locator(SEL.ticketDetail)).toBeVisible();
    });

    test('should attach file to ticket', async ({ page }) => {
      await loginUser(pa
```
