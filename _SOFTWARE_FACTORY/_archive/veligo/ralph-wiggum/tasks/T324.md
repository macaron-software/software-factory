# Task T324: Implement welcome-idfm landing page

**Priority**: P2
**Queue**: TDD
**Type**: implementation

## File
veligo-platform/frontend/src/routes/(landing)/welcome-idfm/+page.svelte

## Description
Implement IDFM welcome page with tenant branding, features, CTA to signup

## Success Criteria
- [ ] Implementation complete
- [ ] cargo check passes (or npm run build for frontend)
- [ ] Unit/integration tests added
- [ ] No new warnings
- [ ] Works with gRPC calls (frontend) or database queries (backend)

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: low
WSJF: 6
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 10:06:47 CET 2026)
```

=== Action Results ===
bash: OK
// Template System - Design System Configuration
// Each template defines a complete visual identity inspired by well-known DS

export type TemplateId = 'sport' | 'material' | 'editorial' | 'minimal' | 'default';

export interface TemplateConfig {
  id: TemplateId;
  name: string;
  description: string;
  inspiration: string;
  preview: string;

  // Layout structure
  layout: {
    headerStyle: 'sticky' | 'fixed' | 'static' | 'transparent';
    headerHeight: string;
    sidebarPosition: 'left' 
```

## Last Failure (Wed Jan 14 10:07:38 CET 2026)
```

---
bash: OK
// Template System - Design System Configuration
// Each template defines a complete visual identity inspired by well-known DS

export type TemplateId = 'sport' | 'material' | 'editorial' | 'minimal' | 'default';

export interface TemplateConfig {
  id: TemplateId;
  name: string;
  description: string;
  inspiration: string;
  preview: string;

  // Layout structure
  layout: {
    headerStyle: 'sticky' | 'fixed' | 'static' | 'transparent';
    headerHeight: string;
    sidebarPosition: 'left' 
```

## Last Failure (Wed Jan 14 10:08:06 CET 2026)
```
cat "veligo-platform/frontend/src/routes/(landing)/+layout@.svelte"</bash>

=== Action Results ===
bash: OK
<script lang="ts">
  /**
   * Landing Layout - Pages d'accueil tenant-spécifiques
   *
   * Ce layout supprime le header/footer du layout root car les pages landing
   * gèrent leur propre navigation et mise en page complète.
   *
   * Pages concernées:
   * - /welcome-idfm
   * - /welcome-nantes
   * - /welcome-lyon
   */
  import '../../app.css';
  import { ThemeProvider } from '$lib/theme';
  import TemplateProvider from '$lib/templates/TemplateProvider.svelte';
  import GdprConsent from '$li
```

## Last Failure (Wed Jan 14 10:08:16 CET 2026)
```
Let me see more of these files and check the shared components:
```bash
head -100 "veligo-platform/frontend/src/routes/(landing)/welcome-lyon/+page.svelte"
```

=== Action Results ===
bash: OK
<script lang="ts">
  import { onMount } from 'svelte';

  // Svelte 5 - Reactive state
  let mounted = $state(false);
  let activeTab = $state<'particulier' | 'entreprise' | 'etudiant'>('particulier');
  let selectedRoute = $state(0);

  // Simulated real-time station data
  let stationData = $state([
    { name: 'Part-Dieu', line: 'B', bikes: 12, nextMetro: 2, status: 'ok' },
    { name: 'Bellecour', line: 'A', bikes: 8, nextMetro: 1, status: 'ok' },
    { name: 'Perrache', line: 'A', bikes: 5,
```

## Last Failure (Wed Jan 14 10:08:34 CET 2026)
```

=== Action Results ===
bash: OK
// Template System - Design System Configuration
// Each template defines a complete visual identity inspired by well-known DS

export type TemplateId = 'sport' | 'material' | 'editorial' | 'minimal' | 'default';

export interface TemplateConfig {
  id: TemplateId;
  name: string;
  description: string;
  inspiration: string;
  preview: string;

  // Layout structure
  layout: {
    headerStyle: 'sticky' | 'fixed' | 'static' | 'transparent';
    headerHeight: string;
    sidebarPosition: 'left' 
```

## Last Failure (Wed Jan 14 10:09:17 CET 2026)
```

=== Action Results ===
bash: OK
<script lang="ts">
  import { onMount } from 'svelte';

  // Svelte 5 - $state() for reactive state
  let scrollY = $state(0);
  let isVisible = $state(false);
  let activeStation = $state<number | null>(null);

  // CO2 Counter - Real-time simulation
  let co2Saved = $state(847.3);
  let cyclists = $state(12450);
  let kmParcours = $state(2.1);

  // Gamification
  let badges = $state([
    { id: 'first-ride', name: 'Première balade', icon: '🚴', earned: true },
    { id: 'eco-warrior', name: 'G
```
