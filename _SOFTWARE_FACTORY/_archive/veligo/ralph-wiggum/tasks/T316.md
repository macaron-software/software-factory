# Task T316: Implement /onboarding complete flow

**Priority**: P1
**Queue**: TDD
**Type**: implementation

## File
veligo-platform/frontend/src/routes/(app)/onboarding/+page.svelte

## Description
Implement complete onboarding: identity verification (FranceConnect), payment setup, preferences. Connect to real APIs

## Success Criteria
- [ ] Implementation complete
- [ ] cargo check passes (or npm run build for frontend)
- [ ] Unit/integration tests added
- [ ] No new warnings
- [ ] Works with gRPC calls (frontend) or database queries (backend)

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: medium
WSJF: 10
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 10:04:39 CET 2026)
```

=== Action Results ===
bash: OK
<script lang="ts">
  import { onMount } from 'svelte';
  import { page } from '$app/stores';
  import type { TenantId } from '$lib/types';
  import { get } from 'svelte/store';
  import { apiClient } from '$lib/api';

  let tenantId: TenantId;
  let loading = true;
  let error: string | null = null;

  onMount(async () => {
    try {
      const url = new URL(window.location.href);
      const subdomain = url.hostname.split('.')[0];
      tenantId = subdomain as TenantId;
      await apiClient.g
```

## Last Failure (Wed Jan 14 10:13:19 CET 2026)
```
    email: string;
    given_name: string;
    family_name: string;
    birthdate?: string;
  }

  const STEPS: OnboardingStep[] = [
    { id: 'identity', title: 'Identity Verification', description: 'Verify your identity with FranceConnect' },
    { id: 'payment', title: 'Payment Setup', description: 'Add your payment method' },
    { id: 'preferences', title: 'Preferences', description: 'Customize your experience' },
    { id: 'complete', title: 'Complete', description: "You're all set!" }
  ];

  let currentStep = 0;
  let loading = false;
  let error: string | null = null;
  let identityVerified = false;
  let identityData: IdentityData | null = null;
  let paymentMethod: PaymentMethod | null = null;
  let preferences: UserPreferences = { notifications: true, newsletter: false, theme: 'sy
```

## Last Failure (Wed Jan 14 10:13:46 CET 2026)
```

=== Action Results ===
bash: OK
<script lang="ts">
  import { onMount } from 'svelte';
  import { page } from '$app/stores';
  import type { TenantId } from '$lib/types';
  import { get } from 'svelte/store';
  import { apiClient } from '$lib/api';

  let tenantId: TenantId;
  let loading = true;
  let error: string | null = null;

  onMount(async () => {
    try {
      const url = new URL(window.location.href);
      const subdomain = url.hostname.split('.')[0];
      tenantId = subdomain as TenantId;
      await apiClient.g
```
