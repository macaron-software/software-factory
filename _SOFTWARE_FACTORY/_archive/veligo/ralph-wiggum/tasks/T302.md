# Task T302: Implement /stations page with map

**Priority**: P0
**Queue**: TDD
**Type**: implementation

## File
veligo-platform/frontend/src/routes/(app)/stations/+page.svelte

## Description
Replace skeleton with real station map using StationMapGeneric component, show real-time availability via gRPC StationService

## Success Criteria
- [ ] Implementation complete
- [ ] cargo check passes (or npm run build for frontend)
- [ ] Unit/integration tests added
- [ ] No new warnings
- [ ] Works with gRPC calls (frontend) or database queries (backend)

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: high
WSJF: 12
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 10:01:04 CET 2026)
```
	import StationFilters from './StationFilters.svelte';

	interfac
---
bash: OK
<script lang="ts">
  import { onMount } from 'svelte';
  import { isModuleEnabled, tenantStore } from '$lib/stores/tenant';

  let loading = $state(true);
  let error: string | null = $state(null);
  let modulesLoaded = $state(false);
  let stationManagementEnabled = $state(false);

  // Update module states when tenant store changes
  $effect(() => {
    const unsubscribe = tenantStore.subscribe((state) => {
      if (state.initialized && state.tenant) {
        modulesLoaded = true;
        st
```

## Last Failure (Wed Jan 14 10:01:53 CET 2026)
```
sed -n '1,200p' "./veligo-platform/frontend/src/routes/(app)/stations/+page.svelte"
</bash>

=== Action Results ===
bash: OK
<script lang="ts">
  import { onMount } from 'svelte';
  import { isModuleEnabled, tenantStore } from '$lib/stores/tenant';

  let loading = $state(true);
  let error: string | null = $state(null);
  let modulesLoaded = $state(false);
  let stationManagementEnabled = $state(false);

  // Update module states when tenant store changes
  $effect(() => {
    const unsubscribe = tenantStore.subscribe((state) => {
      if (state.initialized && state.tenant) {
        modulesLoaded = true;
        st
```

## Last Failure (Wed Jan 14 10:02:53 CET 2026)
```
i

=== LLM Response ===
<think>
The output seems incomplete. Let me try a different approach to read the file.
</think>

<bash>
sed -n '1,150p' veligo-platform/frontend/src/lib/grpc-client.ts</bash>

=== Action Results ===
bash: OK
import { GrpcWebFetchTransport } from '@protobuf-ts/grpcweb-transport';
import type { UnaryCall } from '@protobuf-ts/runtime-rpc';
import { AuthServiceClient } from './grpc/auth/v1/auth.client';
import { ModuleServiceClient } from './grpc/module/v1/module.client';
import { SubscriptionServiceClient } from './grpc/subscription/v1/subscription.client';
import { StationServiceClient } from './grpc/station/v1/station.client';
import { BookingServiceClient } from './grpc/booking/v1/booking.client';
i
```
