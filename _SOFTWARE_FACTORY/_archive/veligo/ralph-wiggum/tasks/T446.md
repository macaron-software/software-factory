# Task T446: Contraste et zoom 200%

**Priority**: P1
**WSJF Score**: 7.5
**Queue**: TDD
**Category**: AO_COMPLIANCE
**Original ID**: AO-RGAA-003
**Phase**: 🎯 PHASE 2: COMPLIANCE AO (Priority P1)
**Section**: 2.4 RGPD & Accessibilité

## Description
Contraste et zoom 200%

## Test File
`ao/rgaa-contrast.spec.ts`

## Estimated Files
2 fichier(s) à modifier

## Success Criteria
- [ ] Test E2E créé/modifié: `ao/rgaa-contrast.spec.ts`
- [ ] Test passe en local
- [ ] Pas de test.skip() sans condition
- [ ] Code review adversarial passé
- [ ] Pas de stubs ou TODOs

## Definition of Done (DoD)
- [ ] Feature complètement implémentée
- [ ] Tests E2E verts
- [ ] Conformité AO vérifiée (si applicable)
- [ ] Documentation mise à jour

## Ralph Status Block
---RALPH_STATUS---
STATUS: FAILED
COMPLEXITY: low
WSJF: 7.5
CATEGORY: AO_COMPLIANCE
ORIGINAL_ID: AO-RGAA-003
---END_RALPH_STATUS---

## Last Failure (Wed Jan 14 13:50:03 CET 2026)
```
</think>

<bash>
cd veligo-platform && sed -n '205,250p' frontend/tests/accessibility.spec.ts</bash>

=== Action Results ===
bash: OK
  test.describe('accessibility - Contrast Ratios', () => {
    test('accessibility - text should have sufficient contrast', async ({ page }) => {
      await page.goto(BASE_URL);

      // Use Axe specifically for color contrast
      const contrastResults = await new AxeBuilder({ page })
        .withRules(['color-contrast'])
        .analyze();

      expect(contrastResults.violations).toEqual([]);
    });

    test('accessibility - links should be distinguishable from text', async ({ page }) 
```

## Last Failure (Wed Jan 14 13:51:48 CET 2026)
```
 * RGAA Accessibility Tests - Level AA Compliance
 *
 * Tests for:
 * - Contrast ratios (WCAG 2.1 AA - 4.5:1)
 * - Keyboard navigation
 * - ARIA labels
 * - Skip links
 *
 * Run with: npm run test:a11y
 */

import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

const BASE_URL = process.env.BASE_URL || 'http://localhost:5173';

// Pages to test
const PAGES_TO_TEST = [
  { path: '/', name: 'Home' },
  { path: '/logi
```

## Last Failure (Wed Jan 14 13:52:57 CET 2026)
```
 *
 * Tests for:
 * - Contrast ratios (WCAG 2.1 AA - 4.5:1)
 * - Keyboard navigation
 * - ARIA labels
 * - Skip links
 *
 * Run with: npm run test:a11y
 */

import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

const BASE_URL = process.env.BASE_URL || 'http://localhost:5173';

// Pages to test
const PAGES_TO_TEST = [
  { path: '/', name: 'Home' },
  { path: '/login', name: 'Login' },
  { path: '/regi
```
