# Task T143: Create E2E Journey - subscriber-onboarding

**Priority**: P1
**Queue**: TDD
**AO_REF**: AO-LYON-JOURNEY
**Tenant**: lyon

## Description
Missing E2E journey: subscriber-onboarding - New TCL user registration

Journey: New TCL user registration

## File to Create
tests/e2e/journeys/lyon-subscriber-onboarding.spec.ts

## Test Template
```typescript
import { test, expect } from '@playwright/test';
import { users } from '../fixtures/users.json';
import { stations } from '../fixtures/stations.json';

const BASE_URL = process.env.LYON_URL || 'https://lyon.veligo.app';

test.describe('[AO-LYON-JOURNEY] subscriber-onboarding', () => {

  test('Complete subscriber-onboarding journey', async ({ page }) => {
    // Step 1: Navigate to starting point
    await page.goto(`${BASE_URL}/`);

    // Step 2: User actions
    // TODO: Implement New TCL user registration

    // Step 3: Verify success
    await expect(page.locator('[data-testid="success-message"]')).toBeVisible();
  });

});

```

## Required Selectors
- Navigation: `[data-testid="nav-*"]`
- Forms: `[data-testid="form-*"]`
- Buttons: `[data-testid="btn-*"]`
- Status: `[data-testid="status-*"]`

## Success Criteria
- [ ] Test file created with proper structure
- [ ] All user steps implemented
- [ ] Selectors use data-testid
- [ ] Test runs without skip
- [ ] AO compliance verified

## Ralph Status Block
---RALPH_STATUS---
STATUS: COMPLETED
COMPLEXITY: medium
WSJF: 8
---END_RALPH_STATUS---
