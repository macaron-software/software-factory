# Task T600: Implémenter get_tenant_modules gRPC

**Priority**: P0
**WSJF Score**: 10.0
**Queue**: TDD
**Domain**: api

## Description
Le endpoint `get_tenant_modules` retourne UNIMPLEMENTED en production.
Cela casse TOUS les sites car le frontend essaie de charger les modules au démarrage.

## Erreur actuelle (console prod)
```
gRPC Error (UNIMPLEMENTED): get_tenant_modules not yet implemented
```

## Fichier source
`veligo-platform/backend/src/bin/tonic-server.rs:696`

```rust
async fn get_tenant_modules(...) -> ... {
    Err(Status::unimplemented("get_tenant_modules not yet implemented"))
}
```

## Solution
1. Lire le tenant_id depuis la requête gRPC
2. Requêter la table `tenant_modules` en DB
3. Retourner la liste des modules actifs pour ce tenant

## Fichiers à modifier
- `veligo-platform/backend/src/bin/tonic-server.rs`
- Ajouter migration si table `tenant_modules` n'existe pas

## Test E2E
```typescript
test('get_tenant_modules returns active modules', async ({ page }) => {
  await page.goto('https://idfm.veligo.app');
  // Vérifier que la console n'a pas d'erreur UNIMPLEMENTED
  const errors = await page.evaluate(() => window.__consoleErrors);
  expect(errors.filter(e => e.includes('UNIMPLEMENTED'))).toHaveLength(0);
});
```

## Definition of Done
- [ ] Endpoint implémenté (pas de stub)
- [ ] Table tenant_modules existe en DB
- [ ] Test E2E passe en local
- [ ] Test E2E passe en staging
- [ ] Test E2E passe en prod
- [ ] Console prod sans erreur UNIMPLEMENTED

## Ralph Status Block
---RALPH_STATUS---
STATUS: COMPLETED
COMPLEXITY: medium
WSJF: 10.0
---END_RALPH_STATUS---
