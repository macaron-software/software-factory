team_id: team-platform-dev
technology: python
phase_type: development
description: Team team-platform-dev — end-to-end collaboration bench
pattern:
  type: sequential
  agents:
  - id: plat-dev-agents
  - id: plat-dev-patterns
  - id: plat-dev-frontend
task: Create a new orchestration pattern called 'consensus' where all agents must agree before proceeding. Define the YAML
  schema, implement the engine, and create UI controls.
checks:
- has_keyword:implement
- has_keyword:test
- length_min:300
expectations:
- Each agent contributes according to their role
- Output is coherent and addresses the full task
- Code or technical content is present where expected
