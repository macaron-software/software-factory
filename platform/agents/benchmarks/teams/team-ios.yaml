team_id: team-ios
technology: ios
phase_type: development
description: Team team-ios — end-to-end collaboration bench
pattern:
  type: hierarchical
  agents:
  - id: mobile_ios_lead
  - id: mobile_ios_dev_ui
  - id: mobile_ios_dev_net
task: Implement biometric authentication (Face ID / Touch ID) with fallback to PIN code. Store credentials in Keychain, handle
  enrollment state, and implement secure session management.
checks:
- has_keyword:implement
- has_keyword:test
- length_min:300
expectations:
- Each agent contributes according to their role
- Output is coherent and addresses the full task
- Code or technical content is present where expected
