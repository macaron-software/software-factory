team_id: team-android
technology: android
phase_type: development
description: Team team-android — end-to-end collaboration bench
pattern:
  type: hierarchical
  agents:
  - id: mobile_android_lead
  - id: mobile_android_dev_ui
  - id: mobile_android_dev_net
task: 'Implement an offline-first sync mechanism for a mobile app: local Room database, background sync worker, conflict resolution
  (last-write-wins with server timestamp), and retry with exponential backoff.'
checks:
- has_keyword:implement
- has_keyword:test
- length_min:300
expectations:
- Each agent contributes according to their role
- Output is coherent and addresses the full task
- Code or technical content is present where expected
